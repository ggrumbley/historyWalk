<div class="col-xs-10 col-xs-offset-1 col-md-3">
  <img ng-src={{exhibit.image}} alt="{{exhibit.exhibit}}" >

  <div class="panel panel-default exDisc">
    <div class="panel-body">
      {{exhibit.exNum}} | {{tour.title}} <br />
      <b>{{exhibit.artist}}</b> <br />
      {{exhibit.title}}
    </div>
  </div>
</div>
